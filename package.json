{
  "name": "nd-toolbox",
  "version": "0.4.0",
  "private": true,
  "description": "Local-first PWA suite of neurodivergent internal-state tools. First module: the Feelings Wheel.",
  "type": "module",
  "scripts": {
    "icons": "node scripts/generate-icons.mjs",
    "changelog": "node scripts/emit-changelog.mjs",
    "a11y:contrast": "node scripts/check-contrast.mjs",
    "a11y:audit": "node scripts/audit-a11y.mjs",
    "a11y": "npm run a11y:contrast && npm run build && npm run a11y:audit",
    "dev": "npm run changelog && vite",
    "build": "npm run a11y:contrast && npm run changelog && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "idb-keyval": "^6.2.1"
  },
  "devDependencies": {
    "axe-core": "^4.12.1",
    "playwright-core": "^1.61.1",
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^0.20.5"
  }
}
